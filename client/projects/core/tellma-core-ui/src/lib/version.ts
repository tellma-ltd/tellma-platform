/**
 * Version marker for `@tellma/core-ui`.
 *
 * The primary entry point carries the cross-cutting, component-free surface
 * (providers, i18n, fonts, forms infrastructure); it is populated in later
 * stages. Components live in per-component secondary entry points
 * (`@tellma/core-ui/input`, `/checkbox`, `/form-field`, `/select`).
 */
export const TM_CORE_UI_VERSION = '0.1.0';
