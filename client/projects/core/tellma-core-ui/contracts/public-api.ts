/*
 * Public API Surface of @tellma/core-ui/contracts.
 *
 * Zero-/low-runtime cross-cutting contract types (spec 0002 §2.1). This
 * entry point stays free of Angular and component imports (enforced by
 * lint) so the future data grid can depend on it without pulling in the
 * components.
 */

export * from './tm-contracts';
