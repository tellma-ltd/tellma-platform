/*
 * Public API Surface of @tellma/core-ui-tokens.
 *
 * The typed TmTokens contract, the brand default preset, and the
 * tokens-to-CSS-variables emitter (populated in a later stage).
 */

export * from './lib/version';
