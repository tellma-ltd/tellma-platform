/*
 * Public API Surface of @tellma/locale-ar.
 *
 * The reference Arabic locale pack: library strings (Transloco scope) plus the
 * self-hosted Noto Sans Arabic subset, wired by provideTellmaLocaleAr()
 * (populated in a later stage).
 */

export * from './lib/version';
